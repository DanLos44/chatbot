
pipeline {
    agent {
        label 'docker'
    }
    environment {
    	DOCKERHUB_CREDENTIALS = credentials('daniellosev-dockerhub')
    	}
    
    stages {
       stage('Build') {
          steps {
             echo 'is this even working'       	
          }
       }

 }
 }



